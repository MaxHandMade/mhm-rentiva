jQuery(window).on('elementor:init', function () {
    // Elementor Editor JS
    // This script is loaded in the Elementor editor panel

    // Example: Dynamic vehicle loading if we were using a custom control
    // Currently, we use standard Select2 controls which Elementor handles well.

    // We can add custom behavior for our widgets here if needed.

    /*
    elementor.hooks.addAction('panel/open_editor/widget/mhm-vehicle-card', function(panel, model, view) {
        // Code to run when Vehicle Card widget settings are opened
    });
    */
});
